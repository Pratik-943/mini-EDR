# Required before agents can enroll. Generate a long random value and keep it private.
EDR_BOOTSTRAP_TOKEN=replace-with-a-long-random-secret

# SQLite is ideal for local development. Use an absolute path in a production service.
EDR_DATABASE_PATH=data/mini_edr.db
EDR_SERVER_NAME=mini-EDR

