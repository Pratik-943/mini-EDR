# This directory stores the compiled agent.exe payload.
# It is automatically populated by the GitHub Actions build workflow.
# Place your compiled agent.exe here to serve it via /download/agent endpoint.
